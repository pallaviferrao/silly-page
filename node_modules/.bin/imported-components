#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*|*MINGW*|*MSYS*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../react-imported-component/bin/imported-components" "$@"
  ret=$?
else 
  node  "$basedir/../react-imported-component/bin/imported-components" "$@"
  ret=$?
fi
exit $ret
